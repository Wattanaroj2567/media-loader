{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && corepack prepare pnpm@11.21.0 --activate && pnpm install",
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/web 2>/dev/null; [ $? -ne 0 ] && exit 1; git diff --quiet HEAD^ HEAD -- apps/web"
}
