{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "[ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ] && exit 1; PREV=\"$VERCEL_GIT_PREVIOUS_SHA\"; git diff --quiet \"$PREV\" HEAD -- apps/web 2>/dev/null; CODE=$?; if [ $CODE -gt 1 ]; then git diff --quiet HEAD^ HEAD -- apps/web 2>/dev/null; CODE=$?; fi; [ $CODE -eq 0 ] && exit 0 || exit 1"
}
